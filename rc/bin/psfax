#!/bin/rc
# AFPL Ghostscript removed in commercial build: psfax needs `gs -sDEVICE=dfaxlow`.
echo 'psfax: gs (AFPL) removed in commercial build' >[1=2]
exit nogs
view=no
stdin=no

switch($1){
case -v
	view=yes
	shift
}

switch($#*){
case 0 1
	echo usage: $0 telephone-number recipient [files]
	exit 0
case 2
	stdin=yes
}

telno=$1
shift

recip=$1
shift

script=/tmp/fax.$pid
header=/tmp/faxh.$pid
user=`{cat /dev/user}
tmp=/tmp/page.$pid
tmpin=/tmp/page.in.$pid
tel=`{grep '\) '$user /lib/tel}
myname=`{echo $tel | sed 's/ \(.*//'}
ext=`{echo $tel | sed 's/.*\) [^ ]* [^ ]* ([^ ]*).*/\1/'}

fn sigint{
	#rm -f $tmp.* $tmpin.* $script $header $header.*
	exit interrupt
	
}

# gs insists on reading its standard input, so we read quit.ps to cut it off.
# (AFPL gs removed: unreachable stub, kept for reference.)
switch($stdin){
case yes
	cat > $tmpin
	# gs -dSAFER ... (removed)
	echo 'psfax: gs (AFPL) removed' >[1=2]; exit nogs
case *
	# gs -dSAFER ... (removed)
	echo 'psfax: gs (AFPL) removed' >[1=2]; exit nogs
}

pages=`{echo $tmp.*|wc -w}

# use delimiters that are unlikely to be supplied in arguments
echo -n s∮FAXddd∮	 >$script
echo -n `{date}		>>$script
echo ∮			>>$script
echo -n s∮FAXFFF∮	>>$script
echo -n $myname 	>>$script
echo ∮			>>$script
echo -n s∮FAXEEE∮	>>$script
echo -n $user 		>>$script
echo ∮			>>$script
echo -n s∮FAXVVV∮	>>$script
echo -n $ext 		>>$script
echo ∮			>>$script
echo -n s∮FAXTTT∮	>>$script
echo -n $recip 		>>$script
echo ∮			>>$script
echo -n s∮FAXfff∮	>>$script
echo -n $telno 		>>$script
echo ∮			>>$script
echo -n s∮FAXPPP∮	>>$script
echo -n $pages 		>>$script
echo ∮			>>$script
sed -f $script /sys/lib/fax/h.ps > $header

# gs -dSAFER ... dfaxlow ... (AFPL gs removed: unreachable stub.)
echo 'psfax: gs (AFPL) removed' >[1=2]; exit nogs

files=()
for(i in $header.* $tmp.*){
	files=($files -f $i)
}

switch($view){
case no
	upas/qer $files /mail/faxoutqueue fax $user $telno < /dev/null
	rx fax /sys/lib/fax/faxgoose
case yes
	page $header.* $tmp.*
}

#rm -f $tmp.* $header $script $header.* $tmpin
