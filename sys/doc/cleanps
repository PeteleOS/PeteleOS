#!/bin/rc

if(! ~ $#* 1){
	echo usage: cleanps infile >[1=2]
	exit usage
}

# NOTE (commercial build): no longer prepends /sys/doc/preamble
# (embedded B&H Lucida fonts, removed).
{
echo %!PS-Adobe-2.0
grep -v '^%%DocumentFonts' $1
} > cleanps.tmp
mv cleanps.tmp $1
