BOOTDIR=../boot
BOOTLIB=$BOOTDIR/libboot.a$O

BOOTFILES=\
	bootauth.$O\
	9aux.$O\	# aux.c renamed to 9aux.c: aux is a reserved name on Windows
	boot.$O\
	bootcache.$O\
	bootip.$O\
	local.$O\
	bootvirtio9p.$O\
	embed.$O\
	settime.$O\
	paq.$O\
	parts.$O\
	printstub.$O\
	usb.$O\

$BOOTLIB(%.$O):N:	%.$O

$BOOTLIB:	${BOOTFILES:%=$BOOTLIB(%)}
	names=`{membername $newprereq}
	ar vu $BOOTLIB $names
	rm $names

$BOOTFILES:	$BOOTDIR/boot.h

%.$O:	$BOOTDIR/%.c
	$CC -I$BOOTDIR $CFLAGS $BOOTDIR/$stem.c
